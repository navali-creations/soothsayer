---
"soothsayer": patch
---

**Fixed:** Overlay drop rows now keep loot filter rarity colors.

When a loot filter is selected as the rarity source, the Overlay now loads the same filter colors as the rest of the app when it opens or refreshes settings.
